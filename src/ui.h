#ifndef UI_H
#define UI_H

	#include <stdio.h>
	#include "colors.h"
	
	// prototypes
	char* get_input(int len, char* prompt);
	void println(char* text);
	void print(char* text);
	void endl();

#endif
